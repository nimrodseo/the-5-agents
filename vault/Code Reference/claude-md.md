---
type: file-reference
file: CLAUDE.md
component: project-root
tags:
  - file-reference
  - root
---

# `CLAUDE.md`

> [!info] File reference
> **Path:** `CLAUDE.md`
> **Component:** project root
> **Owner:** project / human owner — read by every Claude Code session

## What

Project-level instructions Claude Code automatically loads at the start of every conversation in this repo. Currently contains:

1. **Mandatory vault workflow** — directs Claude to follow `obsidian-vault-workflow` at the start and end of every task.
2. **Project Overview** — describes the-5-agents as a content-creation agent team led by a CEO agent (still being defined).
3. **Project Structure** — points to the `.claude/` subdirectories (`agents/`, `skills/`, `commands/`).
4. **Status** — flags that the project is in an early-setup phase.

## Belongs to

The repository owner. Edited collaboratively as the project evolves; not auto-generated.

## Related

- [[_index|vault root]] — vault navigation index
- [[obsidian-vault-workflow]] — the workflow CLAUDE.md mandates
- [[gitignore]] — sibling root-level config
- [[env-example]] — sibling root-level config
