# gpt-image-gen — Skill Pointer

The image generation skill Yuval uses lives at `.claude/skills/gpt-image-gen/SKILL.md`.

That skill wraps the OpenAI Images API (`gpt-image-1`, 1024×1024, medium quality, PNG output).
It requires `OPENAI_API_KEY` to be set in `.env`.
